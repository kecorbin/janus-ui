
<div class="container-fluid">
  <div class="row">
    <div class="col-md-4">
      <div class="card-wrap">
        <div class="card">
          <div class="container">
            <h1> Add A Scan Job </h1>
            <hr>
            <div class="col-md-4">
              <form>
                <div class="form-group">
                  <div class="form-group row">
                    <label for="dest">Destination</label>
                    {{input type="text" id="dest" class="form-control" value=destination placeholder="192.168.1.0/24" }}
                    <small id="emailHelp" class="form-text text-muted">
                      Enter an IP prefix in
                      <a target="_blank" href="https://www.ripe.net/about-us/press-centre/understanding-ip-addressing">
                         CIDR notation
                      </a>
                    </small>
                  </div>
                  <div class="form-group row">
                    <label for="type">Scan Type</label>
                      <select id="type" class="form-control" onchange={{action "selectType" value="target.value"}}>
                        {{#each types as |typeChoice|}}
                          <option value={{typeChoice}} selected={{eq type typeChoice}}>{{typeChoice}}</option>
                        {{/each}}
                      </select>
                      <small id="emailHelp" class="form-text text-muted">
                        A <strong>Quick Scan</strong> gathers IP, MAC, and Vendor information.
                        A <strong>Full Scan</strong> provides a much deeper scan including port information
                      </small>
                  </div>
                  <!-- <button type="submit" class="btn btn-primary btn-lg">Submit</button> -->
                   <button class="btn btn-primary" {{action "createScan"}}>Create</button>
                </div>
              </form>
            </div>
            <div class="col-md-8">
              <div class="row">
                <p>Please enter the destination of your scan in <a href="https://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing">CIDR notation</a></p>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </div>
</div>
