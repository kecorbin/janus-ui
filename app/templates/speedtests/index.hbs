<div class="row">
  <h1>Speed Tests<div class="col-md-4">
     <button class="btn btn-primary" {{action "createSpeedTest"}}>Create Speed Test</button>
    <!-- {{#link-to 'speedtests.add'}}<button class="btn btn-primary" type="button">New Speedtest</button> {{/link-to}} -->
  </div></h1>


</div>
<div class="card">
  <div class="header">
    <div class="col-md-8">
      <h4 class="title">{{title}}</h4>
    </div>
  </div>
  <div class="content">
    {{line-chart data=model.chartdata options=options}}
  </div>
</div>
{{speedtest-results title="Speedtest Results" speedtests=model.speedtests}}

{{outlet}}
